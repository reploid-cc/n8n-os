# PostgreSQL Credentials
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# NocoDB JWT Secret
NC_AUTH_JWT_SECRET=

# Tên miền của bạn (đã được định nghĩa trong docker-compose và nginx conf)
DOMAIN_NAME=n8n.ai-automation.cloud
# Email của bạn để đăng ký SSL với Let's Encrypt
LETSENCRYPT_EMAIL=reploid.cc@gmail.com
# Tên miền cơ sở thực tế
BASE_DOMAIN=ai-automation.cloud
# Subdomain cho n8n
N8N_SUBDOMAIN=

# ==================================================
# === Cấu hình cho n8n WORKER LOCAL kết nối VPS ===
# ==================================================

# --- Kết nối Cơ sở dữ liệu (PostgreSQL trên VPS) ---
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=<IP_HOẶC_DOMAIN_PUBLIC_CỦA_POSTGRES_TRÊN_VPS>
DB_POSTGRESDB_PORT=5432 # Hoặc port PostgreSQL VPS của bạn nếu khác
DB_POSTGRESDB_DATABASE=n8nsupport_vps # Ví dụ: n8nsupport hoặc n8ndb
DB_POSTGRESDB_USER=
DB_POSTGRESDB_PASSWORD=

# --- Khóa Mã hóa (QUAN TRỌNG: PHẢI GIỐNG HỆT VPS) ---
N8N_ENCRYPTION_KEY=

# --- Kết nối Redis (Redis trên VPS) ---
QUEUE_BULL_REDIS_HOST=103.110.57.247
QUEUE_BULL_REDIS_PORT=6379 # Hoặc port Redis VPS của bạn nếu khác
QUEUE_BULL_REDIS_PASSWORD= # Để trống nếu không có
QUEUE_BULL_REDIS_DB=0

# --- Cấu hình n8n cơ bản (trỏ về domain của n8n VPS) ---
# Sử dụng domain n8n.masteryflow.cc như các trao đổi trước hoặc n8n.ai-automation.cloud nếu đó là domain VPS hiện tại
N8N_HOST=103.110.57.247 # Ví dụ: n8n.masteryflow.cc hoặc n8n.ai-automation.cloud
N8N_PROTOCOL=https
WEBHOOK_URL=https://n8n.masteryflow.cc/ # Thay thế <DOMAIN_N8N_TRÊN_VPS>

GENERIC_TIMEZONE=Asia/Ho_Chi_Minh # Nên đồng nhất với VPS

# --- Cấu hình Runner (tương tự VPS) ---
N8N_RUNNERS_ENABLED=true
OFFLOAD_MANUAL_EXECUTIONS_TO_WORKERS=true

# --- Biến để dễ nhận biết môi trường (tùy chọn, không dùng bởi n8n) ---
# N8N_ENVIRONMENT_TYPE=local_worker_for_vps
TZ=Asia/Ho_Chi_Minh